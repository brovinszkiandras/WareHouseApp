<Window x:Class="WH_APP_GUI.EditEmail"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:WH_APP_GUI"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="EditEmail" Height="450" Width="350" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Margin="5">
            <Label Content="Email Address:" Margin="5"/>
            <TextBox x:Name="Email" Margin="5" Height="25"/>
            <Label Content="Api Key:" Margin="5"/>
            <xctk:MaskedTextBox x:Name="ApiKey" Margin="5" Height="25" Mask="0000 0000 0000 0000" VerticalContentAlignment="Bottom" HorizontalContentAlignment="Center"/>
            <Button Content="Done" x:Name="Done" Margin="5" Click="Done_Click"/>
        </StackPanel>
    </Grid>
</Window>
