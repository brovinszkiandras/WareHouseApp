<Page x:Class="WH_APP_GUI.Product.ProductsPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:WH_APP_GUI.Product"
        mc:Ignorable="d"
        Title="ProductsPage"
        d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Add Product" Name="AddProducts" Click="AddProducts_Click" Background="White" Foreground="Black" BorderThickness="1" BorderBrush="Black" Margin="10"/>
        </StackPanel>

        <Grid x:Name="Display" Grid.Column="1">

            <Grid Name="ProductsDisplay" Visibility="Collapsed">
                <ScrollViewer Name="ProductsDiaplay">
                    <StackPanel Name="ProductsDiaplayStackPanel"/>
                </ScrollViewer>
            </Grid>
        </Grid>

        <Frame x:Name="ProductsContent" Grid.ColumnSpan="2" Visibility="Collapsed"/>
    </Grid>
</Page>